description = "Copy text to the host clipboard via OSC 52"
prompt = """
The following text has been sent to the host clipboard using the gemini-clipboard-bridge:
!{${extensionPath}/skills/gemini-clipboard-bridge/scripts/copy.sh {{args}}}
"""
